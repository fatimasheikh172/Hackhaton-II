[project]
name = "todo-console-app"
version = "0.1.0"
description = "A CLI tool to manage tasks during a single session (non-persistent)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.21.0",
    "rich>=14.2.0",
]
